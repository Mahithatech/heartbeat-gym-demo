const menuBtn=document.getElementById('menuBtn');const navMenu=document.getElementById('navMenu');menuBtn.addEventListener('click',()=>navMenu.classList.toggle('show'));
const revealEls=document.querySelectorAll('.reveal');const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('active')})},{threshold:.15});revealEls.forEach(el=>observer.observe(el));
let countersStarted=false;function startCounters(){if(countersStarted)return;document.querySelectorAll('[data-count]').forEach(counter=>{let target=Number(counter.dataset.count);let current=0;let step=Math.max(1,Math.ceil(target/80));let timer=setInterval(()=>{current+=step;if(current>=target){current=target;clearInterval(timer)}counter.textContent=current+'+'},20)});countersStarted=true}const stats=document.querySelector('.stats');new IntersectionObserver(entries=>{if(entries[0].isIntersecting)startCounters()},{threshold:.3}).observe(stats);
function calculateBMI(){const h=Number(document.getElementById('height').value)/100;const w=Number(document.getElementById('weight').value);const result=document.getElementById('bmiResult');if(!h||!w){result.textContent='Please enter valid height and weight.';return}const bmi=(w/(h*h)).toFixed(1);let status='Normal';if(bmi<18.5)status='Underweight';else if(bmi>=25&&bmi<30)status='Overweight';else if(bmi>=30)status='Obese';result.textContent=`Your BMI is ${bmi} (${status}). Visit the gym for a personalized plan.`}
function submitTrial(e){e.preventDefault();const name=document.getElementById('name').value;const phone=document.getElementById('phone').value;const branch=document.getElementById('branch').value;const msg=`Hi, I want a free trial at Heart Beat Gym.%0AName: ${encodeURIComponent(name)}%0APhone: ${encodeURIComponent(phone)}%0ABranch: ${encodeURIComponent(branch)}`;window.open(`https://wa.me/919876543210?text=${msg}`,'_blank')}
